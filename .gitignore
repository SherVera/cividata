# Base de datos local del server Node (no subir datos personales)
censo.db
censo.db-*

# Dependencias / artefactos
node_modules/
dist/
.vercel/

# Secretos
.env
.env.*

# Generado en build desde env vars (no versionar claves)
web/config.js
.vercel

# Supabase CLI — estado local del enlace (no subir; los .sql sí van en git)
supabase/.temp/
supabase/.branches/
.supabase/

# Respaldos locales (pueden contener datos clínicos)
backup/*
