# Base de datos local del server Node (no subir datos personales)
censo.db
censo.db-*

# Dependencias / artefactos
node_modules/
dist/
.vercel/

# Secretos
.env
.env.*

# Generado en build desde env vars (no versionar claves)
web/config.js
.vercel
